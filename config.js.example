require('dotenv').config();

module.exports = {
  TOKEN: process.env.TOKEN || '', // Required - Your Discord bot token from https://discord.com/developers/applications
  GUILD_ID: process.env.GUILD_ID || '', // Optional - Leave blank for global commands, enter server ID for guild-only commands
  MONGODB_URL: process.env.MONGODB_URL || '',  // Optional - MongoDB connection URL
  OWNER_ID: process.env.OWNER_ID || '', // Optional - Your Discord user ID for owner-only commands
  LOGS: {
    COMMANDS: process.env.LOGS_COMMANDS || '', // Optional - Channel ID for command logs, leave blank to disable
    ERROR: process.env.LOGS_ERROR || '' // Optional - Webhook URL for error logs
  },
  LAVALINK: {
    NODES: [{
      identifier: process.env.LAVALINK_IDENTIFIER || 'main', // Node identifier name
      host: process.env.LAVALINK_HOST || 'localhost', // Lavalink server host
      port: parseInt(process.env.LAVALINK_PORT) || 2333, // Lavalink server port
      password: process.env.LAVALINK_PASSWORD || 'youshallnotpass', // Lavalink server password
      secure: process.env.LAVALINK_SECURE === 'true', // Use secure connection (true/false)
      retryAmount: 5,
      retryDelay: 5000
    }],
    DEFAULT_VOLUME: parseInt(process.env.DEFAULT_VOLUME) || 80,
    AUTO_LEAVE: process.env.AUTO_LEAVE !== 'false', // Auto leave voice channel when queue ends (default: true)
    AUTO_LEAVE_DELAY: parseInt(process.env.AUTO_LEAVE_DELAY) || 30000, // Delay before auto leave in milliseconds (default: 30000 = 30 seconds)
    ENABLE_SEARCH_CACHE: true, // Enable/disable search caching
    SEARCH_CACHE_SIZE: 1000, // Max number of cached searches
    SEARCH_CACHE_TTL: 3600000, // Cache duration in ms (3600000 = 1 hour)
    SEARCH_ENGINE: process.env.SEARCH_ENGINE || 'spotify', // Options: youtube, youtubemusic, spotify, soundcloud, deezer
    RETRY_ON_STUCK: true, // Auto retry when track gets stuck
    LOAD_BALANCER: true, // Distribute load between multiple nodes
    ENABLE_HEALTH_MONITOR: true, // Enable node health checks
    HEALTH_CHECK_INTERVAL: 60000, // Check interval in ms (60000 = 1 minute)
    HISTORY_SIZE: 50, // Number of previous tracks to keep
    ENABLE_FILTERS: true // Enable audio filters (nightcore, bassboost, etc)
  },
  PLAYER_CONTROLS: {
    ENABLED: true, // Enable player controls on now playing message
    TYPE: 'menu', // 'buttons' or 'menu' - Choose control type
    CONTROLS: ['pause_resume', 'skip', 'stop', 'volume_down', 'volume_up', 'shuffle', 'loop'] // Available controls to show (order matters for buttons)
  }
};
